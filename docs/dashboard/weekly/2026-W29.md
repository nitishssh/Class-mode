# Weekly Report — 2026-W29

> Generated scaffold: `npm run report:weekly`. Numbers are machine-derived; narrative is founder-written. Every number on this page comes from a real row. Zeros stay zeros.

## Metrics (auto-generated)

# Weekly metrics — 2026-W29 (2026-07-13 → 2026-07-20, exclusive)

| Metric                             | This week  | vs prev | vs baseline |
| ---------------------------------- | ---------- | ------- | ----------- |
| Attendance: active teachers        | 0          | —       | —           |
| Attendance: rows written           | 0          | —       | —           |
| Attendance: class-days marked      | 0          | —       | —           |
| Fees: active staff                 | 0          | —       | —           |
| Fees: rows created                 | 0          | —       | —           |
| Accounts (users/teachers/students) | 14 / 0 / 3 | —       | —           |

> This is the baseline week. Thresholds activate from next week's run.

## Progress this week

- **Thu 16 Jul (multi-agent build day):** all three build-lane fixes done in parallel worktree agents and merged to `w29-build-merge` — fabricated WhatsApp toast removed (#335), NULL school_code fail-closed fix + regression suite (#336), attendance_view/report_view usage events wired and proven with real rows (#337). 429 unit tests + 17 e2e green on the merged branch. **Awaiting one push to deploy.** One-page paid-pilot offer drafted and print-ready (`docs/dashboard/pilot-offer.md/.pdf`). Dev infrastructure: Windows box "wednesday" is now a remote dev server (SSH + docker context over direct Ethernet); per-project `devdb` and `dpull` helpers; e2e now runs against a real dev DB. Discovered home ISP crawling at ~1Mbps (both machines) — check ACT. S1 (principal call) slipped to Friday morning.

- Founding thesis written, adversarially reviewed (office-hours → CEO review → full /autoplan pipeline: 3 Codex passes + 3 independent Claude reviews), and gate-approved. Plan of record lives in `~/.gstack/projects/NitishKumar-ai-Class-mode/`.
- Dashboard, weekly-report engine (`npm run report:weekly`), operating rhythm doc, and programs pipeline shipped (this commit) — objective 3's metrics precondition is 80% done (view-event wiring + Gemini billing remain, due day 14 = Jul 29).
- Two live prod bugs found by review and queued as fix tasks: fabricated "parents notified on WhatsApp" toast; NULL school_code on admin attendance writes.

## Feature behavior

- **The baseline is zero.** No attendance rows, no fee entries, no feature_usage events this week. 14 accounts exist (0 teachers, 3 students). Nothing in prod is being used organically right now — the plan's no-traction branch (re-onboarding experiment, 2-week traction bar) should be assumed active pending the on-ground audit.
- Known probe history: two AI-generation outages produced zero user complaints. Consistent with the zeros above.

## School-facing

- None this week yet. The WTP visit (audit → term report + paid-pilot offer → ask) is the week's remaining must-do; first attempt due before Friday Jul 17, reschedule-within-week rule applies.

## Blocked / at risk

- **TD1:** pilot school language not named — blocks the vernacular pass locale/script/font decisions.
- **Day-14 preconditions (Jul 29):** view-event wiring done 16 Jul (pending deploy with `w29-build-merge`); Gemini billing link remains (needs a payment method) — confirmed still broken 16 Jul (API key rejected in dev logs).
- Zero-teachers-in-prod means the wedge audit may reveal onboarding, not features, as the first problem. Eyes open.

## Next week (max 3 priorities)

1. The school visit: workflow audit + WTP close attempt with offer sheet (objective 1).
2. Honesty fixes (WhatsApp toast, school_code bug) + view-event wiring shipped and verified.
3. Founders Hub application submitted (pays the Azure bill); start entity/DPIIT paperwork.

## Time budget check

School-facing 0% · Build 30% · Ops 70% (planning-heavy founding week — acceptable once, flagged per the rule that build-only weeks get called out)
