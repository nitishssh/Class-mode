# TODOS

- [x] **Learner Mastery Visualization Dashboard** — Shipped as the **Progress** tab in the Learn Hub (`/learn?mode=progress`), reading `learner_mastery` + `review_schedule` via `GET /api/learn/mastery`. Renders mastery percentages, confidence, and due/upcoming review cards.
